# Module context: NAME

Keep this file concise. Link to authoritative documents instead of copying
project history.

## Responsibility

What this module owns and explicitly does not own.

## Public surface

Entry points, domain services, ports, routes, exported types, and user-visible
capabilities consumed by other modules.

## Data and control flow

The shortest useful path through presentation, domain, ports, adapters, and
external systems. Mention cancellation, state ownership, and persistence when
relevant.

## Invariants

Business and architectural rules that must remain true.

## Dependencies

Only direct internal/external dependencies that affect ownership, contracts, or
how this module is tested. Link to another module context when possible.

## Key files

Only the minimum files an agent should inspect first, with one-line reasons.

## Verification

Focused test, lint, build, and browser commands relevant to this module.

## Decisions and traps

Non-obvious constraints, known edge cases, and links to authoritative task,
architecture, and approved design records.
